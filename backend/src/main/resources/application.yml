spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/field_exchange?useUnicode=true&characterEncoding=utf-8
    username: root
    password: root

server:
  port: 8900

mybatis:
  mapper-locations: classpath:mappers/*.xml